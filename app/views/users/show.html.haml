/ = render partial: '/questions/partials/js_helpers'

- if current_user
  %h2
    = @user.name
  %h4
    Score:
    = @user.score
    %br
    Badges:
    %br
    Finished Topics:
    .subtopic-id
      - @user.subtopics.each do |t|
        / - unless t.name
        = t.name



