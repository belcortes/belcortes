- if current_user
  %h1 Subjects

  - @subjects.each do |subject| 
    = link_to topics_path+"?subject_id=#{subject.id}" do
      .subject{class: "#{'finished' if current_user.finished_with?(subject)}"}
        = subject.name
    - if current_user && current_user.admin?    
      .admin
        = link_to('edit', edit_subject_path(subject), class:'edit')
        = link_to('delete', subject_path(subject), class:'delete', method: :delete, confirm: 'Are you sure you want to delete this subject?')
    %br
    %br
    %br
    %br
- else
  Please 
  %a{:href=>"/users/new"}
    Sign-up
  or 
  %a{:href=>"/sessions/new"}
    Login 
  to start learning!


