%h1 Answers

- @answers.each do |answer|
  .answer
    = answer.content
    = answer.is_correct
    = answer.question_id
  - if current_user && current_user.admin?
    = link_to('edit', edit_answer_path(answer))
    = link_to('delete', answer_path(answer), method: :delete, confirm: 'Are you sure you want to delete this answer?')