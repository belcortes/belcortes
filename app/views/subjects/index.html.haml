%h1 Subjects

- @subjects.each do |subject|
  .subject
    = link_to subject.name, topics_path+"?subject_id=#{subject.id}"
  - if current_user && current_user.admin?
    = link_to('edit', edit_subject_path(subject))
    = link_to('delete', subject_path(subject), method: :delete, confirm: 'Are you sure you want to delete this subject?')


