= user.name
= user.email
= link_to('edit', edit_user_path(user))
= link_to('delete', user_path(user), method: :delete, confirm: 'Are you sure you want to delete this user?') unless user == current_user
%br